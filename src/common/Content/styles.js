const drawerWidth = 0;
const paddingTop = 10;

export const styles = theme => ({
    root: {
        paddingTop: paddingTop,
        paddingLeft: 10,
        paddingRight: 10,
    }
});

export default styles;